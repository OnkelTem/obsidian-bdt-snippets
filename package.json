{
  "name": "obsidian-bdt-snippets",
  "version": "0.2.1",
  "description": "Obsidian Better Dark Theme snippets",
  "main": "index.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "node scripts/build.js src dist",
    "postbuild": "path-exists dev/postbuild.sh && dev/postbuild.sh || exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "path-exists-cli": "^2.0.0",
    "prettier": "^3.1.1",
    "rimraf": "^5.0.5",
    "sass": "^1.69.5"
  }
}
